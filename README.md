# nexttest

Minimal **Next.js + Material UI** starter setup.

## Included
- Next.js (App Router)
- React / React DOM
- MUI (`@mui/material`) with Emotion (`@emotion/react`, `@emotion/styled`)

## Run locally
```bash
npm install
npm run dev
```

Then open <http://localhost:3000>.
